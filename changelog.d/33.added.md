Scanned images are now tested against the real OCR engine in CI, including
where each word's redaction box lands, so a change that moved image
redactions off the text they cover would fail before release
